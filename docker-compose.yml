services:
  chorival:
    build: .
    container_name: chorival
    restart: unless-stopped
    ports:
      - "8096:8000"
    volumes:
      - chorival-data:/data
    environment:
      - DB_PATH=/data/chorival.db
      - SECRET_KEY=10ccec532ba3f9e6efb4814df085e286ee5cfd680d646120051168e22ddeb358
      - ADMIN_USERNAME=admin
      - ADMIN_DISPLAY_NAME=Admin
      - ADMIN_PASSWORD=zuck4861

volumes:
  chorival-data:
